conditioning:
  method:  ps_roi_deconv_patch_kl_stochastic #ps_roi_deconv_patch_various_mask # ps_conv
  params:
    scale: 1.0
    deconv_scale: 1.0
    kernel_size: 256
    exp_task: loss_mode_62_turing_random_mask

    threshold_init: 0.3 #0.75
    patch_size: 32
    skip_point : 500

    YS : 1

data:
  name: lensless_ys #lensless_ys_origin 
  root: ./samples/dps_val/ys_flickr_100 #/data3/dps_val/ys_v1 

measurement:
  operator:
    name: lensless_real_voronoi
    kernel_size: 256
    intensity: 0.5

  noise: 
    name: gaussian
    sigma: 0.002